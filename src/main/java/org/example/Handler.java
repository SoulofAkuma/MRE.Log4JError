package org.example;

/**
 * An interface that every handler implements
 * @author Mika Allert
 */
public interface Handler {

    /**
     * Registers all the handler routes to the routing class
     */
    void register();

}
